---
platform: linux

image_resource:
  type: docker-image
  source: {repository: concourse/bosh-cli}

inputs:
- name: resource-tutorial
- name: some-files

run:
  path: cat
  args: some-files/test.txt
  #args:
   # - -e
    #- -c
    #- |
    #  ls -la
     # pwd
      #ls -la
     # cat test.txt
      #echo "current package version:${PACKAGE_VERSION}"
